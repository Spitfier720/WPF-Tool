<Window x:Class="WPF_Tool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <ContextMenu x:Key="TreeViewItemContextMenu">
            <MenuItem Header="Remove"/>
            <MenuItem Header="Refresh"/>
            <MenuItem Header="Save"/>
            <MenuItem Header="Simulate Exception">
                <MenuItem Header="Internal Server Error"/>
                <MenuItem Header="Not Found"/>
                <MenuItem Header="Time Out"/>
            </MenuItem>
        </ContextMenu>
        <Style x:Key="ModernMenuButton" TargetType="Button">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F0F8FF"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#3399FF"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3399FF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#AAA"/>
                                <Setter TargetName="border" Property="Background" Value="#F7F7F7"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#EEE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Custom Menu Bar using StackPanel and Buttons -->
        <StackPanel Orientation="Horizontal" Background="#EEE" VerticalAlignment="Stretch" Grid.Column="1">
            <Button Content="Start Service" Height="30" Margin="10,0" Click="Start_Click" Style="{StaticResource ModernMenuButton}"/>
            <Button Content="Stop Service" Height="30" Margin="10,0" Click="Stop_Click" Style="{StaticResource ModernMenuButton}"/>
            <Button Content="Load Mock File" Height="30" Margin="10,0" Click="Load_Click" Style="{StaticResource ModernMenuButton}"/>
            <Button Content="Clear Log" Height="30" Margin="10,0" Click="Clear_Click" Style="{StaticResource ModernMenuButton}"/>
        </StackPanel>

        <!-- TreeView Sidebar -->
        <ScrollViewer Grid.Row="0" Grid.Column="0" VerticalScrollBarVisibility="Auto" Grid.RowSpan="2">
            <TreeView x:Name="treeView">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="ContextMenu" Value="{StaticResource TreeViewItemContextMenu}" />
                    </Style>
                </TreeView.ItemContainerStyle>

                <TreeViewItem Header="RestDemo">
                    <TreeViewItem Header="/Countries - GET"/>
                </TreeViewItem>
                <TreeViewItem Header="SoapDemo">
                    <TreeViewItem Header="ProfileService - GetProfile"/>
                    <TreeViewItem Header="ProfileService - GetProfile"/>
                </TreeViewItem>
            </TreeView>
        </ScrollViewer>

        <!-- Main Content Area -->
        <Grid Grid.Row="1" Grid.Column="1" Background="LightGray">
            <TextBlock Text="Main Content Area" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
